name: expense_tracker
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.0 <4.0.0'  # Updated SDK version

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.1  # Use only one version of sqflite
  path: ^1.8.0     # Properly indented
  fl_chart: ^0.70.2
  sqflite_common_ffi: ^2.0.0
  sqflite_common_ffi_web: ^0.4.5+4
  logger: ^2.5.0
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true